# AIOps 能力管理平台 Q2 需求单

> 版本：v2.0
> 日期：2026-04-20
> 原则：每个需求是一个面向用户的功能点，前后端联动交付，不按技术层分类

---

### 1. 告警列表与筛选

用户可在告警中心查看实时告警列表，按告警码、资源类型、时间范围、告警级别进行筛选，支持分页和排序。

- 当前：告警列表为 Mock 数据，筛选条件无实际效果
- 交付：后端提供告警分页查询接口，前端 hooks 对接真实 API，筛选/排序/分页全部生效

---

### 2. 告警详情与事件追踪

用户点击某条告警可查看完整详情，包括基本信息、告警事件时间线、历史操作记录。

- 当前：详情 Drawer 展示 Mock 数据，事件和操作记录为假数据
- 交付：后端提供告警详情/事件/操作记录接口，前端 Drawer 展示真实数据

---

### 3. 单条告警 AI 深度分析

用户在告警列表中选择一条告警，点击"AI分析"按钮，触发 AI 诊断场景，返回根因分析、关联告警、处置建议。

- 当前："AI分析"按钮存在但标注"开发中"，点击无实际效果
- 交付：对接告警诊断 Agent/Scene，在 Drawer 中展示 AI 分析结果，可复用 SceneCenter 已有的 Chat 集成能力

---

### 4. 告警批量分析

用户在告警中心勾选多条告警，点击"批量分析"，AI 对这批告警进行分类、提取共性、推荐 Top N 根因。

- 当前：批量操作栏仅支持标记已读，无 AI 分析能力
- 交付：后端提供批量告警分析接口，前端展示分析结果（告警分类、关联关系、根因排序）

---

### 5. 告警聚类定时任务

用户可配置定时任务，让系统每隔指定时间（如30分钟）自动对近一段时间内的告警进行聚类分析，结果通过通知组推送。

- 当前：aiops.md 已规划该场景，但前端无配置入口
- 交付：在告警中心增加"定时聚类"配置界面（时间间隔、告警范围、分析维度、通知方式），任务由后端 Agent 定时执行

---

### 6. 每日告警聚类分析报告

系统每日自动生成告警聚类分析报告，用户可在告警中心查看历史报告，了解当日告警特征、风险趋势、隐患排查建议。

- 当前：无此功能
- 交付：后端每日定时执行聚类分析并存储报告，前端展示报告列表与详情（告警分类统计、新增特征、风险评级）

---

### 7. 资源权限管理

用户可在安全护栏中按资源类型（主机/应用/网关/中间件/数据库）查看和管理 AI 可访问的资源范围，设置哪些资源允许/禁止 AI 操作。

- 当前：5 种资源类型权限面板 UI 完整，但数据全部 Mock
- 交付：后端提供权限配置查询与保存接口，前端各资源类型面板加载真实权限数据，编辑后可保存

---

### 8. 命令规则管理

用户可在安全护栏中管理 AI 执行命令的黑白名单，标记高危命令，配置放行/拦截规则。

- 当前：命令规则 Tab UI 完整，规则列表为 Mock 数据
- 交付：后端提供命令规则 CRUD 接口，前端规则列表从后端加载，新增/编辑/删除规则可生效

---

### 9. AI 操作审计日志

用户可在安全护栏中查看 AI 所有操作的审计日志，按时间、Agent、资源、用户等维度筛选，了解谁在什么时候通过 AI 执行了什么操作。

- 当前：操作日志 Tab 有 UI，但数据为 Mock API layer
- 交付：后端提供审计日志分页查询接口，前端日志列表对接真实数据，支持多维度筛选

---

### 10. AI 审计概览与风险分析

用户可在安全护栏中查看 AI 操作的整体审计概览，包括高危操作统计、被拦截操作分析、按 Agent/用户维度的操作分布，以及 AI 自动标注的风险建议。

- 当前：AI 审计概览 UI 完整（6个子Tab），数据全部 Mock
- 交付：后端提供审计聚合统计接口，前端概览图表展示真实数据；AI 风险标注能力（高危命令识别、操作建议）落地

---

### 11. 运维大盘真实数据展示

用户打开运维大盘可看到真实的场景使用统计、告警趋势、TOP 问题、安全护栏概览等，而非 Mock 数据。

- 当前：大盘 UI 含 ECharts 图表，所有数据为 Mock
- 交付：后端提供统计聚合接口，前端大盘所有卡片和图表加载真实运行数据

---

### 12. 场景中心场景目录动态加载

用户在场景中心看到的场景列表从后端动态加载，支持场景的启用/停用管理，新增场景无需改前端代码。

- 当前：场景目录（agentScenes）为硬编码，新增场景需要改代码重新发布
- 交付：后端提供场景列表查询和启用/停用接口，前端从后端动态加载场景目录

---

### 13. 能力中心 Skills 与 MCP 能力管理

用户可在能力中心统一管理 Skills、标准 MCP、RESTful API 三类能力，查看能力详情与运行状态。

- 当前：MCP 脚本管理已对接真实后端，但 Skills 列表、标准 MCP 列表、RESTful API 列表仍为 Mock
- 交付：三类能力列表对接真实后端 API，支持查看详情与状态管理，与 MCP 脚本管理统一数据来源

---

### 14. 资源评估 Zone 统计与容量预测

用户可在资源评估模块查看各 Zone 的 CPU/内存/存储真实使用情况，以及容量预测趋势。

- 当前：4 个 Tab UI 完整（概览/Zone 详情/容量预测/优化建议），全部 Mock
- 交付：后端提供 Zone 资源统计和容量预测接口，前端加载真实资源数据和预测曲线

---

### 15. 知识反馈审核流程

用户可在反馈管理中查看 AI 生成的知识草稿，进行审核（通过/驳回），通过的知识进入知识库。

- 当前：反馈管理 UI 完整（草稿列表、审核流程、知识模板），全部 Mock
- 交付：后端提供知识反馈列表查询和审核操作接口，前端草稿列表加载真实数据，审核操作可生效

---

### 16. 执行记录查询与详情

用户可通过菜单进入执行记录页面，查看诊断、巡检、预测三类执行记录，点击可查看详细报告（Agent 调用链、MCP 工具调用、根因分析、处置建议）。

- 当前：3 个 Tab UI 完整，Mock API layer，未加入 App.tsx 菜单
- 交付：后端提供执行记录查询与详情接口，前端对接真实数据并加入菜单

---

### 17. 告警数据标准化治理

建立告警统一数据模型，定义标准化字段（唯一 ID、告警码、资源类型、资源 ID、告警级别、内容、发生时间、恢复时间），使 AI 可高效处理告警数据。

- 当前：告警字段不统一，资源类型缺乏标准编码，影响 AI 分析准确率
- 交付：定义告警标准化模型并在后端落地，前端告警列表和详情使用标准化字段渲染

---

### 18. 指标数据标准化治理

建立指标统一数据模型，定义标准化字段（指标名、单位、采集周期、存储策略），使预测场景可基于高质量指标数据运行。

- 当前：指标命名和存储策略不统一，预测场景配置时难以选择合适指标
- 交付：定义指标标准化模型，预测中心场景配置使用标准化指标列表，前端已有 DatasetQualityBadge 可直接使用

---

### 19. MCP 工具安全鉴权统一管理

用户可在统一界面查看所有 MCP 工具的鉴权状态、资源访问范围、操作规则，支持批量设置鉴权策略，与安全护栏权限体系联动。

- 当前：MCP 工具鉴权散落在各模块，无统一管理入口
- 交付：在能力中心或安全护栏增加 MCP 鉴权管理 Tab，后端提供工具鉴权配置接口，前端统一展示和管理

---

### 20. 告警与资源关联关系可视化

用户可在告警详情中查看告警所涉资源的关联拓扑（应用→主机、应用→中间件、应用→网关），快速定位故障影响范围。

- 当前：告警详情仅展示文字信息，无关联关系可视化
- 交付：后端提供资源关联关系查询接口，前端在告警详情中增加关联拓扑图（基于 ECharts graph），展示告警资源的上下游依赖

---

### 21. 深度诊断场景 — CC表空间使用率超限

告警码对应的深度诊断场景：当 CC 表空间使用率超限告警触发时，AI 自动采集表空间使用详情、增长趋势、碎片率等信息，给出扩容或清理建议。

- 计划完成：4月17日

---

### 22. 深度诊断场景 — Oracle 锁

告警码对应的深度诊断场景：当 Oracle 锁告警触发时，AI 自动查询锁类型、阻塞会话、等待事件，定位锁来源并给出解锁建议。

- 计划完成：4月24日

---

### 23. 深度诊断场景 — Oracle 连接数超限

告警码对应的深度诊断场景：当 Oracle 连接数超限告警触发时，AI 自动采集当前连接数、会话分布、慢查询，给出连接优化建议。

- 计划完成：4月24日

---

### 24. 深度诊断场景 — Oracle 索引失效

告警码对应的深度诊断场景：当 Oracle 索引失效告警触发时，AI 自动查询失效索引列表、影响表范围、重建方案，给出索引重建建议。

- 计划完成：4月30日

---

### 25. 深度诊断场景 — Oracle sequence 耗尽

告警码对应的深度诊断场景：当 Oracle sequence 耗尽告警触发时，AI 自动检查 sequence 当前值、剩余容量、使用速率，给出扩容或重置建议。

- 计划完成：4月30日

---

### 26. 深度诊断场景 — ZMQ Topic 消息数积压

告警码对应的深度诊断场景：当 ZMQ Topic 消息积压告警触发时，AI 自动查询 Topic 消息堆积量、消费速率、生产速率，给出扩容或限流建议。

- 计划完成：5月8日

---

### 27. 深度诊断场景 — ZMQ Topic 长时间未消费

告警码对应的深度诊断场景：当 ZMQ Topic 长时间未消费告警触发时，AI 自动查询消费组状态、消费延迟、断点位置，给出恢复消费的建议。

- 计划完成：5月17日

---

### 28. 深度诊断场景 — ZMQ 集群状态异常

告警码对应的深度诊断场景：当 ZMQ 集群状态异常告警触发时，AI 自动查询集群节点分布、Broker 状态、副本同步情况，给出集群恢复建议。

- 计划完成：5月17日

---

### 29. 深度诊断场景 — ZMQ 节点状态异常

告警码对应的深度诊断场景：当 ZMQ 节点状态异常告警触发时，AI 自动查询节点 CPU/内存/磁盘、进程状态、网络连接，给出节点恢复建议。

- 计划完成：5月24日

---

### 30. 深度诊断场景 — ZK 集群状态异常

告警码对应的深度诊断场景：当 ZK 集群状态异常告警触发时，AI 自动查询集群节点角色分布、Leader 状态、Session 数量，给出集群恢复建议。

- 计划完成：5月24日

---

### 31. 深度诊断场景 — ZK 节点状态异常

告警码对应的深度诊断场景：当 ZK 节点状态异常告警触发时，AI 自动查询节点运行时长、连接数、Watch 数量、磁盘使用，给出节点恢复建议。

- 计划完成：5月30日

---

### 32. 深度诊断场景 — ZCACHE 节点状态异常

告警码对应的深度诊断场景：当 ZCACHE 节点状态异常告警触发时，AI 自动查询节点内存使用、Key 数量、命中率、连接数，给出节点恢复或扩容建议。

- 计划完成：5月30日

---

## 交付节奏

| 月份        | 需求                           | 重点                                                         |
| ----------- | ------------------------------ | ------------------------------------------------------------ |
| **4月**     | #1 #2 #7 #8 #21                | 告警查看 + 安全护栏权限/规则 + CC表空间深度诊断              |
| **4月下旬** | #22 #23                        | Oracle锁/连接数深度诊断                                      |
| **4月底**   | #24 #25                        | Oracle索引/sequence深度诊断                                  |
| **5月上旬** | #3 #4 #9 #10 #11 #12 #17 #26   | AI分析落地 + 审计 + 大盘/场景动态化 + 告警治理 + ZMQ积压诊断 |
| **5月中旬** | #27 #28                        | ZMQ未消费/集群异常深度诊断                                   |
| **5月下旬** | #5 #29 #30                     | 告警聚类定时任务 + ZMQ节点/ZK集群深度诊断                    |
| **5月底**   | #31 #32                        | ZK节点/ZCACHE节点深度诊断                                    |
| **6月**     | #6 #13 #14 #15 #16 #18 #19 #20 | 每日聚类报告 + 各模块对接收尾 + 指标治理 + 鉴权管理 + 关联可视化 |

